# Example environment file for the Movie Reservation app
# Copy this to `.env` and fill values as needed. Do NOT commit secrets.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password_here
